---
description: Full tier-calibrated security audit across all twelve concerns
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/audit/SKILL.md` and follow its flow.

`/vibe-sec:audit` is the full orchestrator: classify the tier, run the in-scope
concerns (deferring to external tools when present), and render the four-band report.
Runs the full twelve-concern, tier-calibrated orchestration.
